# @multigent/multigent

Node.js wrapper for the Multigent native CLI.

```bash
npm install -g @multigent/multigent
multigent version
```

The npm package downloads the matching native `multigent` binary from GitHub Releases during `postinstall`.
